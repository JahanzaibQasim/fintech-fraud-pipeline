import random
import json
import random
import time
from datetime import datetime, timezone

from faker import Faker
from kafka import KafkaProducer

fake = Faker()

# # is_high_risk = random.random() < 0.20

# # for i in range(1,101):
# #     is_high_risk = random.random() < 0.20
# #     print( is_high_risk)

# # HIGH_RISK_USER = [f"user{i}" for i in range(100,110)]
# transaction = {
#     "transaction_id": f"tx_{fake.uuid4()[:12]}"
# }


# print(transaction)





